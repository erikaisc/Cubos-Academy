//Método semelhante ao slice; substr(start, length)

//Extrai parte de uma string, retornando uma nova string. O método recebe o índice de início e tamanho da extração, retornando uma nova string.

const texto = "Um texto qualquer";

const parte = texto.substring(3, 5);

console.log(parte);